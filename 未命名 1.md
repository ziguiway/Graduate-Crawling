### 表1 三类模态特征定义、提取方法与工具

|模态|特征定义|提取方法|常用工具/模型|输出特征|时序单位|
|---|---|---|---|---|---|
|文本|词/字级语义与上下文表示，如 token embedding、句向量、情感/关键词标签|分词、归一化、预训练模型编码、滑窗聚合|jieba/HanLP、BERT/RoBERTa、Sentence-BERT、OpenAI Embedding 等|token级、句级、窗口级向量|字/词/句/窗口|
|语音|声学与韵律特征，如 MFCC、F0、能量、语速、embedding|重采样、VAD、分帧加窗、特征提取、模型编码|FFmpeg、librosa、torchaudio、openSMILE、Wav2Vec2/HuBERT|帧级、段级、全局向量|10–40 ms帧/段|
|视觉|面部/姿态/场景视觉特征，如表情、AU、头部姿态、CNN/Video Transformer embedding|抽帧、人脸检测、对齐、跟踪、编码|OpenCV、ffmpeg、MediaPipe、MTCNN/RetinaFace、ResNet/I3D/VideoMAE|帧级、镜头级、片段级向量|帧/秒/镜头|

### 表2 时序对齐核心规则与跨模态时间对应逻辑

|对齐层级|规则|跨模态对应逻辑|输出粒度|
|---|---|---|---|
|统一时间轴|以全局时间戳为基准，统一为秒或毫秒|文本、语音、视觉均映射到同一绝对时间|全局时间轴|
|文本到时间|按 ASR 词时间戳/强制对齐得到词或句起止时间|文本 token/句 ↔ 语音时间段|词/句|
|语音到时间|VAD 分段 + 帧级特征时间戳|语音帧/段 ↔ 视频帧段|帧/段|
|视觉到时间|抽帧时间戳 + 人脸/目标跟踪|视频帧 ↔ 文本/语音窗口|帧/镜头|
|跨模态融合|按时间窗聚合，允许容差对齐|以最小时间单元做最近邻/插值/注意力对齐|窗口级|

### 表3 从原始素材到标准化时序特征的处理流程

| 步骤       | 输入         | 处理内容           | 输出     | 质控点     |
| -------- | ---------- | -------------- | ------ | ------- |
| 1 素材整理   | 原始文本/音频/视频 | 命名规范、编号、格式检查   | 样本清单   | 样本编号唯一  |
| 2 时间基准统一 | 多模态原始文件    | 统一采样率、帧率、时间戳起点 | 全局时间轴  | 时长一致    |
| 3 单模态提取  | 各模态文件      | 文本编码、语音分帧、视频抽帧 | 单模态特征  | 维度一致    |
| 4 时序对齐   | 单模态特征+时间戳  | 词/句、帧/段、镜头对齐   | 对齐特征   | 无时间倒挂   |
| 5 标准化    | 对齐特征       | 归一化、补零、截断、滑窗   | 标准时序特征 | 可复现     |
| 6 文件落盘   | 标准特征       | 按样本/模态存储       | 特征文件   | 可读取、可核验 |

### 表4 特征文件规范与读取方式

|项目|规范|
|---|---|
|存储格式|`.npz` / `.npy` / `.pt` / `.h5` / `.jsonl`（按团队习惯统一）|
|目录结构|`root/sample_id/modality/feature.ext`|
|命名规则|`{sample_id}_{modality}_{feature_type}_{version}.ext`|
|元数据|样本编号、模态、时长、维度、帧率/采样率、对齐粒度、工具版本|
|读取方式|Python: numpy/torch/h5py/pandas；提供 `read_feature(sample_id, modality)`|
|校验方式|文件存在性、shape、时间戳单调性、NaN/Inf 检查|

### 表5 附件1全部100条样本特征提取结果汇总（模板）

> 说明：下表为汇总模板。你需要在“全量原始特征文件”基础上逐条填写。若附件1是100条样本，应保证 100 行齐全。

|样本编号|模态类型|原始有效时长|特征维度|对齐粒度|特征文件路径|备注|
|---|---|---|---|---|---|---|
|S001|文本|00:01:23|768|句级|/features/S001/text.npz||
|S001|语音|00:01:23|80/768|20ms帧/段|/features/S001/audio.npz||
|S001|视觉|00:01:23|512/2048|5fps/帧|/features/S001/video.npz||
|S002|文本||||||
|S002|语音||||||
|S002|视觉||||||
|…|…|…|…|…|…|…|
|S100|文本||||||
|S100|语音||||||
|S100|视觉||||||

**汇总统计建议另加：**

|统计项|数值|
|---|---|
|样本总数|100|
|文本模态样本数||
|语音模态样本数||
|视觉模态样本数||
|三模态齐全样本数||
|平均有效时长||
|特征文件总数||
|缺失/异常文件数||

### 表6 典型样本验证表（模板）

|项目|内容|
|---|---|
|样本编号|S001|
|文本片段|“……”|
|对应语音时段|00:00:12.300–00:00:15.800|
|对应视频帧段|frame 369–474（25fps）|
|文本特征|句向量/ token 序列，shape: [n, d]|
|语音特征|帧级 MFCC/embedding，shape: [T, d]|
|视觉特征|帧级 CNN/表情特征，shape: [F, d]|
|对齐结果|文本词/句 ↔ 语音段 ↔ 视频帧段 一一对应|
|验证结论|时间戳一致，无跨模态错位|

### 表7 可复现性说明表

|项目|内容|
|---|---|
|操作系统||
|Python 版本||
|关键库版本|numpy、torch、librosa、opencv、transformers 等|
|模型名称与版本||
|核心参数|采样率、帧长、帧移、FPS、窗口大小、batch size 等|
|随机种子||
|运行入口|`run_extract.py` / `run_align.py`|
|配置文件|`config.yaml`|
|复现步骤|1) 安装依赖 2) 放置原始素材 3) 运行提取 4) 运行对齐 5) 校验输出|
|预期输出|100条样本的三模态标准特征文件及汇总表|
